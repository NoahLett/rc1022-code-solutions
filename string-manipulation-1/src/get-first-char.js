/* exported getFirstChar */

/* 1. Define a function with one parameter, string
2. Use the charAt() method with index 0 and assign it to a new variable
3. Return the new variable */

function getFirstChar(string) {
  var firstChar = string.charAt(0);
  return firstChar;
}

/* 1. Define getFirstChar(string)
2. Declare variable firstChar and assign string.charAt(0)
3. Return firstChar */
