/* exported head */

/* 1. Define a function with one parameter (an array)
2. Return the first element of the array */

function head(array) {
  return array[0];
}

/* 1. Define head(array)
2. Retrun first element using array[0] */
