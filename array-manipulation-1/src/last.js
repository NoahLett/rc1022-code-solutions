/* exported last */

/* 1. Define a function with one parameter
2. Use .length property - 1 to determine last index of the array and assign it to a variable
3. Return the array at index of the variable declared above */

function last(array) {
  var lastIndex = array.length - 1;
  return array[lastIndex];
}

/* 1. Define last(array)
2. Declare variable lastIndex and assign array.length - 1
3. return array[lastIndex] */
