/* exported setValue */

/* 1. Define a function with 3 paramenters: object, key, and value
2. Add property by using object['property'] = value */

function setValue(object, key, value) {
  object[key] = value;
}

/* 1. Define setValue(object, key, value)
2. Add a property and value: object[key] = value */
