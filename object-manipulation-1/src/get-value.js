/* exported getValue */

/* 1. Define a function with two parameters, object and key
2. Declare a variable and assign object.key
3. Return variable */

function getValue(object, key) {
  var value = object[key];
  return value;
}

/* 1. Define getValue(object, key)
2. Declare variable value and assign object[key]
3. return value */
